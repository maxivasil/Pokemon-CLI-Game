#include "utils.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>


size_t max(size_t a, size_t b) {
    return a > b ? a : b;
}

size_t min(size_t a, size_t b) {
    return a < b ? a : b;
}