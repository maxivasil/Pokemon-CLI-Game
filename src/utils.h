#ifndef UTILS_H_
#define UTILS_H_
#include <stdlib.h>
#include <stdbool.h>
#include <stdio.h>
#include <string.h>

size_t max(size_t a, size_t b);

size_t min(size_t a, size_t b);

#endif // UTILS_H_
